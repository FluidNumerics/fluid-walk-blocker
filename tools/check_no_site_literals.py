#!/usr/bin/env python3
"""IP-hygiene gate -- PLACEHOLDER.

Milestone 1 replaces this with the real scanner: structural patterns kept in
the repo, the customer term list kept outside it. Until then this exits 0
and says so, so a green CI check is never mistaken for a scan.
"""
import sys

sys.stderr.write("ip-hygiene: gate not yet implemented (Milestone 1); "
                 "nothing was scanned\n")
sys.exit(0)
